@page "/not-found"
@layout MainLayout

<style>
    /* --- ESTILO NOT FOUND --- */
    .not-found-container {
        min-height: 70vh;
        padding: 2rem;
    }

    .error-visual {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .error-code {
        font-size: 8rem;
        font-weight: 900;
        color: #e2e8f0; /* Gris muy claro */
        margin: 0;
        letter-spacing: -5px;
    }

    .error-icon-wrapper {
        position: absolute;
        background: white;
        width: 80px;
        height: 80px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        border: 4px solid #f1f5f9;
    }

    .error-icon {
        font-size: 2.5rem;
        color: var(--primary); /* El violeta #6366f1 de tu sidebar */
    }

    /* Botón que combina con tu Sidebar */
    .btn-primary-custom {
        background-color: var(--primary);
        color: white;
        border: none;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
    }

        .btn-primary-custom:hover {
            background-color: #4f46e5;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(99, 102, 241, 0.4);
        }

    .btn-outline-secondary {
        border-radius: 10px;
        border: 1px solid #e2e8f0;
        background: white;
        color: #64748b;
        font-weight: 500;
    }

        .btn-outline-secondary:hover {
            background-color: #f8fafc;
            color: #1e293b;
        }
</style>

<div class="not-found-container d-flex flex-column align-items-center justify-content-center">
    <div class="error-visual">
        @* Número 404 con estilo de cristal *@
        <h1 class="error-code">404</h1>
        <div class="error-icon-wrapper">
            <i class="bi bi-search error-icon"></i>
        </div>
    </div>

    <h2 class="fw-bold text-dark mt-4">¿Te has perdido?</h2>
    <p class="text-muted text-center mb-4 px-3" style="max-width: 450px;">
        El documento o la página que buscas no está aquí. Puede que haya sido movida, eliminada o que el enlace sea incorrecto.
    </p>

    <div class="d-flex gap-3">
        @* <a href="/documents" class="btn btn-primary-custom px-4 py-2">
            <i class="bi bi-house-door me-2"></i> Ir al Inicio
        </a> *@
        <button class="btn btn-outline-secondary px-4 py-2" onclick="history.back()">
            <i class="bi bi-arrow-left me-2"></i> Volver atrás
        </button>
    </div>
</div>